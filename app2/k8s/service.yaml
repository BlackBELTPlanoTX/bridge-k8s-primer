apiVersion: v1
kind: Service
metadata:
  name: frontend-two-svc
  namespace: lesson-bonus
spec:
  type: NodePort
  selector:
    app: frontend-two
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
